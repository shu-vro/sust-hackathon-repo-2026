# Required for LLM investigation (optional — rule-based fallback runs without it)
OPENROUTER_API_KEY=your_openrouter_api_key

# Server
PORT=8000
HOST=0.0.0.0
NODE_ENV=development
APP_VERSION=0.1.0

# Input guardrails — set to false to skip the LLM pre-screen on complaints
ENABLE_LLM_GUARDRAIL=true
